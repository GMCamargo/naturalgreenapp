import React from 'react'
import { Text, StyleSheet, SafeAreaView } from 'react-native'

const styles = StyleSheet.create({
    texto: {
        fontSize: 30,
        justifyContent: "center"
    }
})


export default props => (
    <SafeAreaView>
        <Text>Tela errada</Text>
    </SafeAreaView>

)